Weather API

1. While Taking API see and handle the errors.
2. Call Back Function:

If our program is compeletly synchrous we can get value to the function and out of the function.
But when our function does something asychronous(like settimeout function and more) that not the 
real case. Now we take the callback and call the callback with the value that we want to send it back.


Template Engine...
1. We will use template engine to make dynamic pages.
We will use Handle Bars.